package com.adibarra.enchanttweaker;

/** Stores the Flame level captured when a projectile is created. */
public interface FlameLevelAccess {
    int enchanttweaker$getFlameLevel();
    void enchanttweaker$setFlameLevel(int level);
}
